import resume from './resume.pdf';

function Resume(){

    return(
        // <section>
        //     <a href={resume} download>Click</a>
        //     <embed src={resume}/>
        // </section>
    );
}

export default Resume;